# PostgreSQL
POSTGRES_DB=jobtracker
POSTGRES_USER=jobtracker
POSTGRES_PASSWORD=jobtracker_dev
POSTGRES_PORT=5432

# Backend exposed on the host
BACKEND_PORT=3000

# Frontend exposed on the host
FRONTEND_PORT=8080

# Authentication session lifetime in seconds (7 days)
AUTH_SESSION_TTL_SECONDS=604800
